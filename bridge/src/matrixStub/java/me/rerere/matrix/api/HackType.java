package me.rerere.matrix.api;

/**
 * Compile-time stub of Matrix's HackType. NOT bundled into the jar; at runtime the real
 * Matrix class is used. Kept in sync with the published Matrix API.
 */
public enum HackType {
    KILLAURA,
    HITBOX,
    SPEED,
    FLY,
    BADPACKETS,
    FASTUSE,
    FASTBOW,
    FASTHEAL,
    BLOCK,
    SCAFFOLD,
    JESUS,
    INVENTORY,
    VELOCITY,
    CHAT,
    INTERACT,
    AUTOFISH,
    PHASE,
    AUTOBOT,
    ELYTRA,
    VEHICLE
}
